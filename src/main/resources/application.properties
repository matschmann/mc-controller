# Server configuration
server.port=8080

# Logging configuration
logging.level.com.example.musiccast=DEBUG
logging.level.org.springframework.web.client=DEBUG

# HTTP client timeout settings
spring.mvc.async.request-timeout=10000
